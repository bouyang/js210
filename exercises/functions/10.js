logValue();

function logValue() {
  console.log('Hello, world!');
}

// Hello world. Function definitions are hoisted so when logValue is invoked on line 1, it is able to
// correctly identify that there is a corresponding function available to execute